repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    # Match the version you specified in your pyproject.toml
    rev: v0.15.11 
    hooks:
      # Run the linter and sort imports
      - id: ruff
        args: [ --fix ]
      # Run the formatter
      - id: ruff-format